# 持久化配置
appendonly yes
appendfsync everysec
aof-use-rdb-preamble yes

# RDB 快照策略
save 900 1
save 300 10
save 60 10000

# 数据目录（容器内固定为 /data）
dir /data

# 内存策略（避免 embeddings 撑爆内存）
maxmemory 512mb
maxmemory-policy allkeys-lru

# 绑定和端口
bind 0.0.0.0
port 6379